## 문제 설명

`1`부터 `N`까지의 **홀수만** 골라 합을 구해 말하기로 출력하세요.

### 입력
- `N`: 1 이상의 자연수 (채점 시 자동 설정)

### 출력
- 1~N 범위의 홀수들의 합

### 예시
`N = 5` → 출력: `9` (1+3+5)
`N = 10` → 출력: `25` (1+3+5+7+9)
`N = 1` → 출력: `1`

### 힌트
- `흐름`의 `N번 반복` 블록
- `계산`의 `나누기의 나머지` 블록으로 홀수 판정 (나머지 `1`)
- 합 변수는 직접 만들어 쓰세요
