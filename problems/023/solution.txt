현재합 = 숫자들[0]
최댓값 = 숫자들[0]
for 현위치 in range(1, len(숫자들)):
    if 숫자들[현위치] > 현재합 + 숫자들[현위치]:
        현재합 = 숫자들[현위치]
    else:
        현재합 = 현재합 + 숫자들[현위치]
    if 현재합 > 최댓값:
        최댓값 = 현재합
Entry.print(최댓값)
