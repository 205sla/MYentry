if 숫자들[0] >= 숫자들[1]:
    최댓값 = 숫자들[0]
    두번째 = 숫자들[1]
else:
    최댓값 = 숫자들[1]
    두번째 = 숫자들[0]
for i in range(2, len(숫자들)):
    현재 = 숫자들[i]
    if 현재 > 최댓값:
        두번째 = 최댓값
        최댓값 = 현재
    elif 현재 > 두번째:
        두번째 = 현재
Entry.print(두번째)
