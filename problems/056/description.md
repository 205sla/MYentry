## 문제 설명

변수 `점수`가 **60 이상**이면 `합격`, 아니면 `불합격`을 말하기로 출력하세요.

### 입력
- `점수`: 0 이상의 정수 (채점 시 자동 설정)

### 출력
- `합격` 또는 `불합격` 말하기

### 예시
`점수 = 85` → 출력: `합격`
`점수 = 59` → 출력: `불합격`
`점수 = 60` → 출력: `합격` (경계 포함)

### 힌트
- `흐름`의 `만약 ... 이라면, 아니면` 블록
- `판단`의 `≥` 또는 `>`, `=` 블록 조합
