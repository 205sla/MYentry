최빈값 = 숫자들[0]
최대빈도 = 0
for 현위치 in range(len(숫자들)):
    값 = 숫자들[현위치]
    빈도 = 0
    for 내부위치 in range(len(숫자들)):
        if 숫자들[내부위치] == 값:
            빈도 = 빈도 + 1
    if 빈도 > 최대빈도:
        최빈값 = 값
        최대빈도 = 빈도
    elif 빈도 == 최대빈도 and 값 < 최빈값:
        최빈값 = 값
Entry.print(최빈값)
