## 문제 설명

변수 `N`의 **제곱** (`N × N`)을 말하기로 출력하세요.

### 입력
- `N`: 정수 (채점 시 자동 설정)

### 출력
- `N × N` 결과를 말하기

### 예시
`N = 3` → 출력: `9`
`N = 7` → 출력: `49`
`N = -4` → 출력: `16`

### 힌트
- `계산` 카테고리의 곱셈 블록에 `N`을 두 번 넣으면 됩니다
- `생김새` 카테고리의 `말하기` 블록으로 출력
