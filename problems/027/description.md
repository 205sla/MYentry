## 문제 설명

**오름차순으로 정렬된** 리스트 `숫자들`에서 **두 항목의 합이 `목표`와 같아지는 쌍**을 찾아 `작은값+큰값` 형식의 문자열로 말하기 출력하세요. 없으면 `없음`을 말하기.

정답 쌍이 둘 이상 존재할 수 있는 경우에는 **투 포인터 알고리즘**이 양 끝에서 안쪽으로 좁혀가며 먼저 만나는 쌍을 정답으로 합니다.

### 입력
- `숫자들`: **오름차순 정렬된** 정수 리스트 (채점 시 자동 설정)
- `목표`: 정수 (채점 시 자동 설정)

### 출력
- 찾은 쌍 `작은값+큰값` 또는 `없음`을 말하기

### 예시
입력: `숫자들 = [1,3,5,7,9]`, `목표 = 10` → 출력: `1+9`
입력: `숫자들 = [2,4,6,8]`, `목표 = 12` → 출력: `4+8`
입력: `숫자들 = [1,2,3,4,5]`, `목표 = 100` → 출력: `없음`

### 힌트
- `흐름`의 `...인 동안 반복` 블록, `판단`의 `그리고`·`=`·`<`, `계산`의 `합치기` 블록
- 두 포인터(왼쪽·오른쪽 위치), 합, 찾음 플래그용 보조 변수는 직접 만들어 쓰세요

<details>
<summary>자세한 접근법 (투 포인터)</summary>

- 왼쪽 포인터를 `1`, 오른쪽 포인터를 `숫자들의 항목 수`로 초기화. 찾음 플래그를 `0`으로
- 왼쪽 < 오른쪽이고 아직 못 찾은 동안 반복:
  - 합 = `숫자들의 왼쪽번째 항목 + 숫자들의 오른쪽번째 항목`
  - 합 = 목표 → 찾음을 `1`로 (반복 종료 신호)
  - 합 < 목표 → 왼쪽 포인터를 `+1` (합을 키움)
  - 그 외 → 오른쪽 포인터를 `-1` (합을 줄임)
- 반복 종료 후:
  - 찾음 = 1 이면 `(왼쪽 항목) + "+" + (오른쪽 항목)` 합치기 말하기
  - 아니면 `없음` 말하기

</details>
