## 문제 설명

변수 `N`의 **절댓값**을 말하기로 출력하세요. 절댓값은 부호를 제거한 크기입니다.

### 입력
- `N`: 정수 (채점 시 자동 설정)

### 출력
- `|N|` 값을 말하기

### 예시
`N = -7` → 출력: `7`
`N = 5` → 출력: `5`
`N = 0` → 출력: `0`

### 힌트
- `흐름`의 `만약 ... 이라면` 블록으로 `N < 0` 판정
- 음수면 `-N`(부호 뒤집기)으로 양수로 변환
- `판단`의 `<` 블록
